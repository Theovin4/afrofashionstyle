import Link from "next/link";
export default async function PaymentSuccess({searchParams}:{searchParams:Promise<Record<string,string>>}){const q=await searchParams;const order=`AF-${new Date().getFullYear()}-${String(Date.now()).slice(-6)}`;return <main className="status-page"><div className="status-card"><div className="success-mark">✓</div><span className="eyebrow">Payment confirmed</span><h1>Your story is on its way.</h1><p>Thank you for choosing Afro.Fashionstyle. Your order has been received and a confirmation is on its way to your inbox.</p><div className="receipt"><span>Order number <b>{order}</b></span><span>Payment <b>{q.gateway||"Secure payment"}</b></span><span>Total <b>{q.currency||"USD"} {q.total||"—"}</b></span></div><div className="status-actions"><Link className="button primary" href={`/orders/track?order=${order}`}>Track your order</Link><Link className="text-link" href="/">Continue shopping →</Link></div></div></main>}
