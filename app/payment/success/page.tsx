import Link from "next/link";
export default async function PaymentSuccess({searchParams}:{searchParams:Promise<Record<string,string>>}){const q=await searchParams;const order=`AF-${new Date().getFullYear()}-${String(Date.now()).slice(-6)}`;return <main className="status-page"><div className="status-card"><div className="success-mark">✓</div><span className="eyebrow">Payment confirmation preview</span><h1>Your story is on its way.</h1><p>This page demonstrates the completed-order experience. A real order is confirmed only after Flutterwave or PayPal verifies payment through a secure webhook.</p><div className="receipt"><span>Reference <b>{order}</b></span><span>Payment option <b>{q.gateway||"Secure payment"}</b></span><span>Preview total <b>{q.currency||"USD"} {q.total||"—"}</b></span></div><div className="status-actions"><Link className="button primary" href={`/orders/track?order=${order}`}>Preview tracking</Link><Link className="text-link" href="/">Continue shopping →</Link></div></div></main>}
